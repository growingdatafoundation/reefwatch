'use strict';
var app = require('../../server/server');

module.exports = function(Volunteer) {
/*    Volunteer.SaveVolunteers = function(volunteers, cb) {
        console.log('SaveVolunteers', volunteers.toObject());
        var volunteersP = app.models.Volunteer;

        volunteersP.destroyAll({"observationId":});
       observationP.create(observationData, function(err, observation) {
            console.log('ObservationID', observation.id);
            SelectedSite.observationId = observation.id;
            selectedSiteP.create(SelectedSite, function (err, selectedSite) {
                console.log('SelectedSiteID', selectedSite.id);
                cb(null, selectedSite);
            });
        });

    }

    Volunteer.remoteMethod('SaveVolunteers', {
          accepts: [{arg: 'observationId', type: 'string'},{arg: 'data', type: 'Volunteer', http: { source: 'body' }}],
          returns: [{arg: 'data', type: 'Volunteer', root: true}]
    });*/

};
